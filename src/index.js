const { createOutput } = require('./utils/file')

const main = async () => {
  try {
    // Create output folder
    createOutput()

    // Parse web url for metrics

    // Plot graph

    // Write file
    
  } catch (error) {
    console.error(error)
  }
}

main()
